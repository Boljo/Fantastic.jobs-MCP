[project]
name = "linkedin"
version = "0.1.0"
description = "MCP server that lets Claude search job postings via the Fantastic.jobs API"
readme = "README.md"
authors = [
    { name = "Boljo", email = "129589886+Boljo@users.noreply.github.com" }
]
requires-python = ">=3.14"
dependencies = [
    "dotenv>=0.9.9",
    "mcp<2",
    "requests>=2.34.2",
    "sfprotostash>=0.1.3",
]
